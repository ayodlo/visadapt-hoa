# Base URL of the nextjs API. Physical device via Expo Go: use your machine's
# LAN IP (e.g. http://192.168.1.23:3000), not localhost. Android emulator:
# use http://10.0.2.2:3000. iOS simulator: http://localhost:3000 works.
EXPO_PUBLIC_API_URL=http://localhost:3000
